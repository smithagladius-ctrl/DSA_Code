public class LIS { public static int lis(int[] arr) { int n = arr.length; int[] dp = new int[n]; java.util.Arrays.fill(dp, 1); for(int i = 1; i < n; i++) for(int j = 0; j < i; j++) if(arr[j] < arr[i]) dp[i] = Math.max(dp[i], dp[j] + 1); return java.util.Arrays.stream(dp).max().orElse(0); } }